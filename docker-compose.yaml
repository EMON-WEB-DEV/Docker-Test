version: '3.9'
services:
  app:
    build: ./app
    environment:
      - HOSTNAME=app-container
      - PLATFORM=linux



  nginx:
    image: nginx:latest
    ports:
       - "8080:80"
    volumes:
      - ./nginx/default.conf:/etc/nginx/conf.d/default.conf
    depends_on:
      - app